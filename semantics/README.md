# semantics/ — placeholder

**Filled on Day 2.** This will hold the **semantic layer**: `meta_*` tables (metric definitions,
table/column descriptions, join hints) that live inside the database and give the NL2SQL agent the
context the Day 1 naive spike was missing. Empty on purpose for now.
