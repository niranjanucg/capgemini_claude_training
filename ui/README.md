# ui/ — placeholder

**Filled on Day 4.** This will hold the **Streamlit UI**: a simple chat-style front end where a
service manager types a question and sees the answer, the generated SQL, and the returned rows.
Empty on purpose for now.
