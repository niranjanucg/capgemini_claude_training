# api/ — placeholder

**Filled on Day 3.** This will hold the **FastAPI service** that exposes the NL2SQL agent over HTTP
(a `/ask` endpoint: question in, answer + SQL out). Empty on purpose for now.
