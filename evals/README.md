# evals/ — placeholder

**Filled on Day 2.** This will hold the **eval harness**: a set of natural-language questions paired
with expected results, used to measure the NL2SQL agent's **execution accuracy** as we add the
semantic layer and guardrails. The example questions in `db/schema.sql` seed this set.
Empty on purpose for now.
